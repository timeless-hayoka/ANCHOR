# Local lab authorization evidence

This file satisfies the evidence reference required by scope confirmation records.
It documents that the target is an authorized local training repository, not a
production deployment.
